import styled from 'styled-components';
import { Box } from 'grommet';

export const CommentContainer = styled(Box).attrs({
  align: 'center',
  direction: 'row-responsive',
  gap: 'small',
  pad: 'small',
  align: 'start',
  border: true,
})``;
